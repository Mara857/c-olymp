#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
	vector <int> a;
	int b;
	for(int i=0;i<3;i++)
	{
		cin>>b;
		a.push_back(b);
	}
	sort(a.begin(),a.end());
	cout << a[1];
	return 0;
}
